{
	"version": "0.1.0",
	// List of configurations. Add new configurations or edit existing ones.
	"configurations": [
		{
			"name": "Attach",
			"type": "node",
			"request": "attach",
			// TCP/IP address. Default is "localhost".
			"address": "localhost",
			"protocol": "legacy",
			// "trace": "all",
			// Port to attach to.
			"port": 6010,
			"sourceMaps": false,
			"outFiles": ["${workspaceRoot}/../eslint/server/**/*.js"]
		}
	]
}